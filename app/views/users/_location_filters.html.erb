<div>
  <h3>Locations</h3>
  # DB searches shouldn't be performed in a view. This should be moved to the User model.
  # review the changes with me on Monday
  <% @users.group('location').select('location').where("location IS NOT NULL").each do |user| %>
    <button class="filters" id="<%= user.location %>"><%= user.location %></button><br><br>
  <% end %>
</div>
