<% flash.each do |key, value| %>
    <%= value %>
<% end %>

<%= render partial: 'admins/todo', locals: { users: @users } %>

<br><br><br>

<%= render partial: 'admins/students', locals: { students: @students } %>

<br><br><br>

<%= render partial: 'admins/employers', locals: { employers: @employers } %>

<br><br><br>

<%= render partial: 'admins/interests', locals: { interests: @interests } %>

<br><br><br>

<%= render partial: 'admins/companies', locals: { companies: @companies } %>

<br><br><br>

<%= render partial: 'admins/cohorts', locals: { cohorts: @cohorts } %>

<br><br><br>

<%= render partial: 'admins/questions', locals: { answers: @answers } %>


<%= render partial: 'admins/create_users_and_groupables' %>
