<% flash.each do |key, value| %>
    <%= value %>
<% end %>

<div class="admin-nav">
<ul>
  <li><%= link_to "To-do", "#to-do" %></li>
  <li><%= link_to "Students", "#students"%></li>
  <li><%= link_to "Employers", "#employers"%></li>
  <li><%= link_to "Employer-to-Student Interests", "#employer_to_student_interests"%></li>
  <li><%= link_to "Student-to-Company Contact", "#company_contacts"%></li>
  <li><%= link_to "Companies", "#companies"%></li>
  <li><%= link_to "Cohorts", "#cohorts"%></li>
  <li><%= link_to "Questions", "#questions"%></li>
  <li><%= link_to "Create Users/Groups", "#create"%></li>
</ul>
</div>

<div class="admin-tables">

<a name="to-do">
<div class="admin-portion to-do">
<%= render partial: 'admins/todo', locals: { users: @users, companies: @companies } %>
</div>

<a name="students">
<div class="admin-portion">
<%= render partial: 'admins/students', locals: { students: @students } %>
</div>

<a name="employers">
<div class="admin-portion">
<%= render partial: 'admins/employers', locals: { employers: @employers } %>
</div>

<a name="employer_to_student_interests">
<div class="admin-portion">
<%= render partial: 'admins/employer_to_student_interests', locals: { interests: @interests } %>
</div>

<a name="company_contacts">
<div class="admin-portion">
<%= render partial: 'admins/company_contacts', locals: { company_contacts: @company_contacts } %>
</div>

<a name="companies">
<div class="admin-portion">
<%= render partial: 'admins/companies', locals: { companies: @companies } %>
</div>

<a name="cohorts">
<div class="admin-portion">
<%= render partial: 'admins/cohorts', locals: { cohorts: @cohorts } %>
</div>

<a name="questions">
<div class="admin-portion">
<%= render partial: 'admins/questions', locals: { answers: @answers } %>
</div>

<a name="create">
<div class="admin-portion">
<%= render partial: 'admins/create_users_and_groupables' %>
</div>

</div>
